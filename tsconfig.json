{
  "compilerOptions": {
    "module": "commonjs", /* Specify what module code is generated. */
    "declaration": false, /* Generate .d.ts files from TypeScript and JavaScript files in your project. */
    "removeComments": true, /* Disable emitting comments. */
    "emitDecoratorMetadata": true, /* Emit design-type metadata for decorated declarations in source files. */
    "experimentalDecorators": true, /* Enable experimental support for TC39 stage 2 draft decorators. Default is false. */
    "allowSyntheticDefaultImports": true, /* Allow 'import x from y' when a module doesn't have a default export. */
    "target": "es2017", /* Set the JavaScript language version for emitted JavaScript and include compatible library declarations. Default es2016*/
    "sourceMap": false, /* Create source map files for emitted JavaScript files. */
    "outDir": "./built", /* Specify an output folder for all emitted files. */
    "baseUrl": "./", /* Specify the base directory to resolve non-relative module names. */
    "incremental": true, /* Save .tsbuildinfo files to allow for incremental compilation of projects. */
    "skipLibCheck": true, /* Skip type checking all .d.ts files. */
    "strictNullChecks": true, /* When type checking, take into account 'null' and 'undefined'. */
    "noImplicitAny": false, /* Enable error reporting for expressions and declarations with an implied 'any' type. */
    "strictBindCallApply": false, /* Check that the arguments for 'bind', 'call', and 'apply' methods match the original function. */
    "forceConsistentCasingInFileNames": false, /* Ensure that casing is correct in imports. Default is true.*/
    "noFallthroughCasesInSwitch": false, /* Enable error reporting for fallthrough cases in switch statements. Default is true. */
    "types": [
      "node"
    ],
    "allowJs": true,
  },
  "exclude": [
    "node_modules", "dist", "**/*spec.ts", "test"
  ],
  "include": ["src/**/*", "./server.ts"],
}